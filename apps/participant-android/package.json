{
  "name": "@travel-bingo/participant-android",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cap:sync": "cap sync android",
    "cap:open": "cap open android",
    "apk:debug": "cap sync android && cd android && gradlew.bat assembleDebug",
    "apk:install": "cap sync android && cd android && gradlew.bat installDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.9.3"
  }
}
